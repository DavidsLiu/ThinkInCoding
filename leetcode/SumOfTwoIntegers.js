/**
* Calculate the sum of two integers a and b, but you are not allowed to use the operator + and -.
*
*  Example:
*  Given a = 1 and b = 2, return 3.
*
*/

/**
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
var getSum = function(a, b) {
  
};


/**
* 位运算 &与运算  | 或运算 ^异或运算 ~取反运算 >>右移 <<左移
*
*
*
*
*/

var result = getSum(1,2);
console.log(result);
